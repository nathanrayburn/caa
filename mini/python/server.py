
# Get userpassword and HKDF the password to store it in database

# Login

# Register
def register(username, password, publicKey, cipheredPrivateKey):
    # check if user exist
    # create user & save keys
def login(username, password):
    # check if user exist
    # ensure security for timing attacks
    # hash(password) == password_from_db

# Change password

# Store messages
